"""Ad platform adapters (Meta, TikTok, future: YouTube, Google).

Each subpackage exposes one platform's auth, publishing, and insights logic.
Shared primitives (token encryption, base error types) live under ``_base``.
"""
